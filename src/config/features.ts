// Feature flags for staged launch.
// Flip these on once the underlying integration is live.

// PayPal Commerce Platform: Partner approval pending.
// Until approved, hide all PayPal CTAs to avoid broken flows.
export const PAYPAL_ENABLED = false;
