// Feature flags for staged launch.
// Flip these on once the underlying integration is live.

// PayPal Commerce Platform — enabled (Partner approval landing imminently).
export const PAYPAL_ENABLED = true;
