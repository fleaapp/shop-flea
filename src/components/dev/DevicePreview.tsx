/**
 * Device preview overlay — permanently disabled across all environments
 * (web, PWA, Safari, Capacitor native). Kept as a no-op export so existing
 * imports continue to work.
 */
export default function DevicePreview() {
  return null;
}
